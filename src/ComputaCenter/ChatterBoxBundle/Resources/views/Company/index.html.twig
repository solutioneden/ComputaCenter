{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Company list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Logo</th>
                <th>Website</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('chatterbox_company_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.logo }}</td>
                <td>{{ entity.website }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('chatterbox_company_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('chatterbox_company_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('chatterbox_company_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
